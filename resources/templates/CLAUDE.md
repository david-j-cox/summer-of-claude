# Project instructions (Claude Code)

Claude Code loads this file automatically. The shared instructions live in
`AGENTS.md`; the line below imports them so both tools follow the same rules without
duplicating them.

@AGENTS.md

## Claude Code specific notes

- Add anything here that is specific to Claude Code (for example, which skills to
  prefer). Keep the shared rules in `AGENTS.md`.
